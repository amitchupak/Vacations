gitHub link - https://github.com/amitchupak/Vacations

**Run the app on the course AWS server:** see [AWS-SETUP.md](AWS-SETUP.md).

**Run on this PC with Docker:** `docker compose up -d --build` — then open **http://localhost:5002** or the public **https://…trycloudflare.com** URL from `docker compose logs cloudflared` (works even when AWS ports are closed).
